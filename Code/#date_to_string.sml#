(*toma una fecha y retorna un string de la forma
Enero 20, 2015 (por ejemplo). Use el operador ^ para concatenar strings y la funciÃ³n
Int.toString de la biblioteca de SML para convertir un int a string. Para producir la parte
del mes, no use un montÃ³n de condicionales; en su lugar, use una lista con 12 strings y la
funciÃ³n que se utilizÃ³ en el problema anterior. Por consistencia, ponga una coma despuÃ©s
del dÃ­a y los meses con su inicial mayÃºscula: Enero, Febrero, etc*)

fun get_nth (listString : string  list, n : int) =
    if n = 1
    then hd(listString)
    else get_nth(tl(listString),n-1)


fun date_to_string (date: (int*int*int)) =
    let val months = ["Enero","Febrero","Marzo","Abril","Mayo","Junio","Julio","Agosto","Setiembre","Octubre"
		           ,"Noviembre","Diciembre"]
	in
	(get_nth(months,#2 date) ^ " " ^ (Int.toString (#3 date)) ^ ", " ^ (Int.toString(#1 date)))
    end 
 
 val test7 = date_to_string (2013, 6, 1)